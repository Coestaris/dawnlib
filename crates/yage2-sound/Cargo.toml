[package]
name = "yage2-sound"
version = "0.1.0"
edition = "2021"

[features]
default = ["cpal"]
cpal = ["dep:cpal"]

[dependencies]
yage2-core = { path = "../yage2-core" }
cpal = { version = "0.16.0", optional = true }
log = "0.4.27"

